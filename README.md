# ovn-tools
Operational tooling for OVN — currently: unified DHCP/lease-sync hook for updating OVN logical router ports, SNAT, and routes when an uplink's address changes dynamically (dhclient, dhcpcd, networkd-dispatcher adapters into one generic core).
